import java.util.ArrayList;

public class GerenciaEmprestimo {

GerenciaEmprestimo emprestimo = new GerenciaEmprestimo();

    private boolean cadrastroEmprestimoExistente(String id, String cpf) {
        if (getId(id) == null && getCpf) {
            return false;
        }
        return true;
    }
    public void cadastroDeEmprestimo (Usuario, Livro) {
        if (!cadastroEmprestimoExistente(cpf, id)) {
            cadastros.add(new Usuario(cpf, id) {
            });
        }
    }

}
